export function CatchAllBadge() {
	return (
		<span className="inline-flex shrink-0 items-center rounded-full bg-[var(--surface-hover)] px-2 py-0.5 text-xs font-medium text-[var(--warning)]">
			キャッチオール
		</span>
	);
}
